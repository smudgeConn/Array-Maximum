//Write a program that creates the following array, then calculates and shows
//the array's maximum value.

const values = [3, 11, 7, 2, 9, 10];
let sum = 0;
values.forEach((value) => {
  if (value > sum) {sum = value}
  document.querySelector('body').innerText = sum;
})
